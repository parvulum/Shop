<h1>Каталог товаров</h1>

<table>
 <!-- <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Image url</th>
      <th>Price</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead> 
  <tbody> -->
    <% @products.each do |product| %>
      <tr class="<%= cycle('list_line_odd', 'list_line_even') %>">
        <td>
          <%= image_tag(product.image_url, class: 'list_image') %>
        </td>
        <td class="list_description">
          <dl>
            <dt><%= product.title %></dt>
            <dd><%= truncate(strip_tags(product.description), lenght: 900) %></dd>
          </dl> 
        </td>  
        <!--<td><%= product.title %></td>
        <td><%= product.description %></td>
        <td><%= product.image_url %></td>
        <td><%= product.price %></td> -->
        <td class= "list_actions">
          <%= link_to 'Информация о товаре', product %></br>
          <%= link_to 'Редактировать', edit_product_path(product) %></br>
          <%= link_to 'Удалить', product, method: :delete, data: { confirm: 'Вы уверены?' } %>
        </td>
      </tr>
    <% end %>
 <!-- </tbody> -->
</table>

<br />

<%= link_to 'Добавить товар', new_product_path %>
